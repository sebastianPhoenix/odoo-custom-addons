<odoo>
    <!-- Eigene Settings-View für LLM Assistant -->
    <record id="view_llm_assistant_res_config_settings" model="ir.ui.view">
        <field name="name">llm.assistant.res.config.settings</field>
        <field name="model">res.config.settings</field>
        <field name="arch" type="xml">
            <form string="LLM Assistant Einstellungen">
                <sheet>
                    <group string="LLM Assistant">
                        <group>
                            <field name="llm_base_url"/>
                            <field name="llm_api_key" password="1"/>
                            <field name="llm_model_id"/>
                        </group>
                        <group>
                            <button name="action_fetch_llm_models"
                                    type="object"
                                    string="Verfügbare Modelle laden"
                                    class="btn-primary"/>
                        </group>
                    </group>
                </sheet>
            </form>
        </field>
    </record>

    <!-- Action für das Settings-Fenster -->
    <record id="action_llm_assistant_config" model="ir.actions.act_window">
        <field name="name">LLM Assistant</field>
        <field name="type">ir.actions.act_window</field>
        <field name="res_model">res.config.settings</field>
        <field name="view_mode">form</field>
        <field name="view_id" ref="view_llm_assistant_res_config_settings"/>
        <field name="target">new</field>
        <field name="context">{'module': 'odoo_llm_assistant'}</field>
        <field name="groups_id" eval="[(4, ref('base.group_system'))]"/>
    </record>

    <!-- Menüeintrag unter Einstellungen -->
    <menuitem id="menu_llm_assistant_config"
              name="LLM Assistant"
              parent="base.menu_config"
              action="action_llm_assistant_config"
              sequence="100"
              groups="base.group_system"/>

    <!-- Chat-Widget-Template kann bleiben, das ist unabhängig von Settings -->
    <!-- ... -->
</odoo>
